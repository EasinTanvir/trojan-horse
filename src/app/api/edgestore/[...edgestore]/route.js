import { edgeStoreHandler } from "@/lib/edgestore";

/** EdgeStore's upload/confirm endpoints. Server-only by construction. */
export const GET = edgeStoreHandler;
export const POST = edgeStoreHandler;
